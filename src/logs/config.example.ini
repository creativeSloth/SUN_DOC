; Example config — copy to config.ini on first checkout.
;     cp src/logs/config.example.ini src/logs/config.ini
;
; Paths below are relative to the working directory the app is started
; from. The README instructs running with `cd src && python main.py`, so
; relative paths resolve as ../example_data/... → <repo>/example_data/...
;
; If you start the app from a different cwd, either:
;   - re-pick the paths via the Settings dialog (Settings → Pfade), or
;   - replace the values below with absolute paths.

[Pfade]
source_path = ../example_data/source
target_1_path = ../example_data/target
template_1_path =
template_2_path =
target_2_path =
config_path = ./src/logs/config.ini

[Abfrage]
sql1 = SELECT article_no, article_name, amount FROM articles;
sql2 =

[Server]
server = localhost:5432
user = sundoc_user
password = sundoc123
db_name = sundoc_db
